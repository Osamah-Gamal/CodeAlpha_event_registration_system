<%
layout = 'layouts/admin-layout';
pageTitle = 'Admin Dashboard';
currentPage = 'dashboard';
%>

<!-- الإحصائيات -->
<div class="row">
    <div class="col-md-4 mb-4">
        <div class="card text-white bg-primary stat-card">
            <div class="card-body text-center">
                <i class="fs-1">👥</i>
                <h2 class="card-title mt-2"><%= stats.total_users %></h2>
                <p class="card-text">Total Users</p>
            </div>
        </div>
    </div>
    <div class="col-md-4 mb-4">
        <div class="card text-white bg-success stat-card">
            <div class="card-body text-center">
                <i class="fs-1">📅</i>
                <h2 class="card-title mt-2"><%= stats.total_events %></h2>
                <p class="card-text">Total Events</p>
            </div>
        </div>
    </div>
    <div class="col-md-4 mb-4">
        <div class="card text-white bg-info stat-card">
            <div class="card-body text-center">
                <i class="fs-1">📋</i>
                <h2 class="card-title mt-2"><%= stats.total_registrations %></h2>
                <p class="card-text">Total Registrations</p>
            </div>
        </div>
    </div>
</div>

<!-- Quick Actions -->
<div class="row mt-4">
    <div class="col-md-6 mb-3">
        <div class="card h-100">
            <div class="card-body text-center">
                <i class="fs-1 text-primary">👥</i>
                <h4 class="card-title mt-3">User Management</h4>
                <p class="card-text">Manage all system users, update roles, and remove accounts.</p>
                <a href="/admin/users" class="btn btn-primary">Manage Users</a>
            </div>
        </div>
    </div>
    <div class="col-md-6 mb-3">
        <div class="card h-100">
            <div class="card-body text-center">
                <i class="fs-1 text-success">📅</i>
                <h4 class="card-title mt-3">Event Management</h4>
                <p class="card-text">Create, edit, and manage all events in the system.</p>
                <a href="/admin/events" class="btn btn-primary">Manage Events</a>
            </div>
        </div>
    </div>
</div>

<!-- Recent Activity -->
<div class="row mt-5">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">📈 System Overview</h5>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-6">
                        <h6>Recent Users</h6>
                        <ul class="list-group">
                            <% users.slice(0, 5).forEach(function(user) { %>
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <%= user.username %>
                                <span class="badge bg-<%= user.role === 'admin' ? 'warning' : 'secondary' %> rounded-pill">
                                    <%= user.role %>
                                </span>
                            </li>
                            <% }); %>
                        </ul>
                    </div>
                    <div class="col-md-6">
                        <h6>Upcoming Events</h6>
                        <ul class="list-group">
                            <% events.slice(0, 5).forEach(function(event) { %>
                            <li class="list-group-item">
                                <strong><%= event.title %></strong><br>
                                <small class="text-muted">
                                    <%= new Date(event.date).toLocaleDateString() %> • <%= event.location %>
                                </small>
                            </li>
                            <% }); %>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>